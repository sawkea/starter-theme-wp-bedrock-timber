{% extends "base.twig" %}

{% block content %}
	<div class="container">
		<h2>{{page.title}}</h2>
		
		{% for post in posts %}
			{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
		{% endfor %}
		<p>Accueil de votre site</p>
		
		
	</div>
{% endblock %}
